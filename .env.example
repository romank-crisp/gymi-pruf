DATABASE_URL=postgres://localhost:5432/gymi_pruf_dev
PAYLOAD_SECRET=change-me-to-a-random-32-char-string

# Anthropic API — used by the generation pipeline (scripts/generate.ts).
# Not required to boot the admin UI.
ANTHROPIC_API_KEY=

# Seed script — creates this admin on first boot. Dev-only convenience.
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=changeme123
